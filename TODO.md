# NIDS-Agent Run TODO

## Plan Steps:
- [x] Step 1: Install Python dependencies (`pip install -r requirements.txt`)
- [x] Step 2: Start backend server (`uvicorn src.api.main:app --reload --port 8000`)
- [x] Step 3: npm deps up to date + dev server running (`http://localhost:5173/`)

**Backend URL:** http://localhost:8000  
**Dashboard URL:** http://localhost:5173 (auto-opens in browser)

